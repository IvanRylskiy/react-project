import React from 'react';

import './button-form-add.scss';

const ButtonFormAdd = props => {
  return (
    <button className="button-form-add" {...props}>
      +
    </button>
  );
};

export default ButtonFormAdd;
